import { Box } from "@mui/material";

const Header = () => {
  return (
    <Box
      sx={{
        backgroundColor: "#264653",
        color: "#f4a261",
        fontSize: "30px",
        textAlign: "center",
        padding: "30px",
        marginBottom: "70px",
      }}
    >
      Header
    </Box>
  );
};

export default Header;
